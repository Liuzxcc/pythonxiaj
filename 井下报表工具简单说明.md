# 进度跟踪报表工具 · 使用说明

给**使用者**看的简明版。开发者视角的完整文档见同目录 `README.md`。

---

## 一、这个工具做什么

你手上有一批**设计明细表**（地质设计 / 工程设计 / 工艺设计 …），每张表的文件名里
写着它属于哪个阶段、是「已完成」还是「审核中」。

工具做的三件事：

1. **认文件** —— 看文件名就知道这是「地质设计-已完成」还是「工艺设计-审核中」；
2. **取数据** —— 已完成的表取「完成日期」，审核中的表取「当前审核人」；
3. **填大表** —— 按井号匹配，把值写进《井下作业设计节点跟踪大表》对应阶段那一列。

**默认直接改原表、且不生成任何新文件。** 工具把值直接填进《井下作业设计节点跟踪大表.xls》
对应格子（**合并单元格、字体、边框等格式全部保留**，改动过的单元格标红方便你扫一眼），
不另存副本、不写备份、不出报告——跑完源目录里**只有被改的这张表**。
界面上点「🔄 一键写入原表」即按规则直接转换，无需任何确认。

> 想要备份 / 试运行 / 副本 / 调整列？这些高级选项**仅命令行可用**（见第六节），日常用图形界面点按钮即可。

---

## 二、准备工作

### 1. 文件放在同一个文件夹里

```
井下作业报表生成/
├── 常规气地质设计-已完成.xls      ← 明细表
├── 常规气地质设计-审核中.xls      ← 明细表
├── 常规气工艺设计-已完成.xls      ← 明细表（有就放，没有就跳过）
└── 井下作业设计节点跟踪大表.xls   ← 要被填充的目标表
```

### 2. 文件名必须写对

格式：`<任意前缀><阶段>-<状态>.xls`

| 位置 | 可填的内容 |
|------|-----------|
| 阶段 | 地质设计、工程方案审查、工程设计、工艺设计、修前工程及概算完成情况、修井作业开工时间、修井作业完工时间 |
| 状态 | 已完成、审核中 |

✅ 正确示例：`常规气地质设计-已完成.xls`、`常规气工艺设计-审核中.xls`
❌ 认不出来：`地质设计汇总.xls`（没写状态）、`地质设计-已驳回.xls`（状态不在上面两类中）

> ⚠️ **必须是 `.xls` 老格式**。`.xlsx` 虽然能被识别，但工具**读不出里面的数据**，
> 会当成空表跳过。Excel/WPS 里「另存为 → Excel 97-2003 工作簿(*.xls)」即可。

> **Windows 用户注意**：文件名里不能有 `\ / : * ? " < > |` 这 9 个符号，
> 工具会直接把它挑出来告诉你，不会静默跳过。

### 3. 表格本身不用改

只要明细表里有「井号」列，以及下面两列之一就行：

- **已完成**的表 → 要有「完成日期」列
- **审核中**的表 → 要有「当前审核人」列

列在第几列无所谓，工具按**表头文字**找，不认列号。

---

## 三、怎么用（图形界面）

1. **双击程序**打开主窗口。

2. **确认路径与源目录**：
   - 源目录（**可多选**）—— 放明细表的文件夹，点「＋ 添加目录」可加多个，点「－ 删除选中」移除
   - 跟踪大表 —— 《井下作业设计节点跟踪大表.xls》

3. **选节点（多选）**：界面里「同步节点」一栏列出了全部 7 个阶段，载入跟踪大表后会
   **自动识别哪些节点真实存在**——存在的可勾选，不存在的自动置灰。只勾你想同步的，
   不勾的节点本次跳过。默认全勾。

4. **点「🔄 一键写入原表」**，看底部「执行日志」。工具会按规则直接把井号进度写入
   《井下作业设计节点跟踪大表.xls》——**不弹任何开关、不生成任何 `.bak` / diff / 日志**，
   合并单元格/字体/边框等原表格式全部保留，改动的单元格标红方便你扫一眼。

> **界面已极简化**：原本的「实际写入 / 谨慎模式 / 保留备份 / 允许覆盖 / 调整列」等开关全部移除。
> 这些高级选项仅在命令行下可用（`--apply` / `--copy-mode` / `--report` / `--allow-recheck-overwrite` /
> `--restructure`），日常用图形界面点按钮就行。

> 支持的 7 个节点：**地质设计 / 工程方案审查 / 工程设计 / 工艺设计 /
> 修前工程及概算完成情况 / 修井作业开工时间 / 修井作业完工时间**。

---

## 四、看懂输出结果

### diff 对照表（CSV，Excel 直接双击打开）

| 列 | 含义 |
|----|------|
| 井号 / sheet / 行号 | 改的是哪张表、哪一行的哪口井 |
| 阶段 / 目标列 | 填的是哪个阶段的哪一列 |
| **改前 / 改后** | 重点看这两列 |
| 动作 | `write` 写入 / `skip` 跳过 / `warn-dup-well` 重复井号告警 |
| 说明 | 为什么写、为什么不写 |

### 「跳过」不代表出错

常见的跳过原因都写在「说明」列里，都是**有意为之**：

| 说明 | 意思 | 要管吗 |
|------|------|--------|
| 日期相同，幂等跳过 | 表里已经是这个日期了 | 不用管，重复跑也一样 |
| 原值日期更新，保留 | 表里已有更晚的日期，不往回改 | 通常不用管 |
| 明细无值，跳过以免清空 | 明细表里这口井没填日期 | **值得看一下**——明细表是否漏填 |
| 已有完成日期，审核中不覆盖 | 防止已完成被退回成审核中 | 除非真有回退，否则不用管 |
| 同 sheet 同阶段出现重复井号 | 大表里这口井出现了两行 | **必须人工确认**填哪一行 |

---

## 五、点完按钮后会发生什么

点「🔄 一键写入原表」后，工具会**按规则直接把井号进度写入《井下作业设计节点跟踪大表.xls》**：

1. **直接改写原表本身**——不另存、不备份、不出报告；
2. **合并单元格、字体、边框等格式全部保留**（用 xlutils 整表克隆后只动目标格子）；
3. **被改过的单元格标红**，方便你扫一眼确认。

> ✅ 因为是原地写入且格式保全，你用 WPS 打开原表核对无误即可，**无需再手动替换**。
> **源目录里只有被改的这张表，没有任何 `.bak` / diff / 日志**——真正零新文件。

> **想保留一份 `.bak` 备份**？图形界面已极简化不支持，走命令行加 `--report` 即可（见第六节）。
> **想另存 `.synced-` 副本（原表不动）**？命令行加 `--copy-mode`。
> **想先试运行、不动文件**？命令行不挂 `--apply` 即可。

> ⚠️ 这份副本是用 xlwt 整表重建的，**合并单元格和条件格式会丢失**。
> 流程是：用 WPS 打开 `.synced-` 文件 → 核对无误 → 再手动替换原表。

---

## 六、命令行用法（可选）

不想开窗口的话：

```bash
# 试运行（推荐先跑这个）
python main.py --cli --source "/path/to/井下作业报表生成"

# 确认后直接写入原表
python main.py --cli --source "/path/to/井下作业报表生成" --apply

# 多个源目录（合并扫描）
python main.py --cli --source "/path/a" --source "/path/b" --apply

# 只同步指定节点（可多次 --stage）
python main.py --cli --source "/path/to/井下作业报表生成" --stage 地质设计 --stage 工艺设计 --apply

# 谨慎模式：不改原表，只另存 .synced 副本
python main.py --cli --source "/path/to/井下作业报表生成" --apply --copy-mode

# 默认 = 零新文件（直接改原表，不生成 .bak / diff / 日志）
python main.py --cli --source "/path/to/井下作业报表生成" --apply

# 想要备份 + 核对报告时，显式开启 --report
python main.py --cli --source "/path/to/井下作业报表生成" --apply --report

# 第一次整理旧表（补工艺设计两列）
python main.py --cli --source "/path/to/井下作业报表生成" --restructure
```

---

## 七、常见问题

**Q：日志说「没有可处理的明细文件」？**
文件名里没同时命中阶段和状态关键字。对照第二节检查一下命名。

**Q：日志列了一堆「✗ 扩展名不支持」？**
正常的 —— 源目录里的 mp4、txt、跟踪大表本身都会被列出来，只是告诉你没被采用。

**Q：提示「缺少『实际完成』列，已回退到『当前进度』」？**
你在用旧版大表。勾选「列结构调整」跑一次就好（只需一次，之后再跑会提示无需调整）。

**Q：明细表里同一口井有两行，取哪个？**
取**流程次数**最大那一行。这是按业务流程推的，一般没问题。

**Q：能撤销吗？**
图形界面是「真正零新文件」模式，**直接改写原表且不会留 `.bak-` 备份**。
需要可回退的保险就改走命令行加 `--report`（在报告目录落 `.bak-` 备份）；不想动原表就加 `--copy-mode`（原表不动、另存 `.synced-` 副本）。

**Q：文件名看着对，但一条记录都没同步？**
多半是 `.xlsx` 格式 —— 工具能认出文件名，但读不出 .xlsx 里的数据。
另存为 `.xls`（Excel 97-2003）再跑。

**Q：Windows 上双击没反应？**
大概率是 `xlrd` 装成了 2.0 以上版本（读不了 .xls）。
命令行执行 `pip install xlrd==1.2.0` 再试。

---

> 本说明为**使用者版本**，开发者视角的完整文档见同目录 `README.md`。
